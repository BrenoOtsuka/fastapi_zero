name: Pipeline
on: [push, pull-request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Instalar o Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
